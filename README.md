# egg-biz-router

A plugin for controller router decorator in eggjs

## Usage

```
$ npm install egg-biz-router
```

add to plugin

```typescript
// config/plugin.ts

export default {
  bizRouter: {
    enable: true,
    package: 'egg-biz-router',
  }
}
```
